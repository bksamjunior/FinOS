# Merged FinOS Scaffold (for future expansion)

This file keeps an expanded scaffold plan (backend app structure, routers, models, services, migrations, etc.)
so the repo can stay minimal now and expand later.

(We can fill this in together next.)
